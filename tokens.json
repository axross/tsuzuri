{
  "_comment": "Machine-readable manifest of every {{TOKEN}} in this template. Drives init.sh. Installed skills carry no tokens, so every entry here belongs to the harness hooks, the CI workflows, AGENTS.md, or the README seed. 'optional: true' tokens belong to an integration the project may not have; during INIT you either keep+fill them or delete the section that uses them AND remove the row here — init.sh apply refuses to run while any listed token has no value, whether or not it still occurs in the tree. See INIT.md.",
  "tokens": [
    { "name": "PROJECT_NAME", "optional": false, "description": "Project / repo name", "example": "acme-web" },
    { "name": "PROJECT_OVERVIEW", "optional": false, "description": "One-line goal / overview", "example": "Internal dashboard for fleet operations." },
    { "name": "PROJECT_KIND", "optional": false, "description": "Kind of project", "example": "web app" },
    { "name": "PRIMARY_LANGUAGE", "optional": false, "description": "Main language", "example": "TypeScript" },
    { "name": "APP_FRAMEWORK", "optional": false, "description": "App framework / runtime", "example": "Next.js" },
    { "name": "PACKAGE_MANAGER", "optional": false, "description": "Package manager binary (single binary; hooks call `command -v` on it)", "example": "npm" },
    { "name": "LINTER", "optional": false, "description": "Linter", "example": "ESLint" },
    { "name": "FORMATTER", "optional": false, "description": "Formatter. If the project has no dedicated formatter, set this to the linter's autofix (e.g. \"ESLint (eslint --fix)\") and see INIT Step 3.", "example": "Prettier" },
    { "name": "UNIT_TEST_FRAMEWORK", "optional": true, "description": "Unit test framework, or 'none'", "example": "Vitest" },

    { "name": "E2E_TEST_FRAMEWORK", "optional": true, "description": "E2E test framework", "example": "Playwright" },
    { "name": "CMS_OR_DATA_LAYER", "optional": true, "description": "Data / content layer", "example": "Prisma" },
    { "name": "HOSTING_PLATFORM", "optional": true, "description": "Hosting / deploy platform", "example": "Vercel" },

    { "name": "INSTALL_CMD", "optional": false, "description": "Install dependencies", "example": "npm install" },
    { "name": "DEV_CMD", "optional": false, "description": "Start dev server", "example": "npm run dev" },
    { "name": "BUILD_CMD", "optional": true, "description": "Production build (drop if no build step)", "example": "npm run build" },
    { "name": "START_CMD", "optional": true, "description": "Start built app", "example": "npm run start" },
    { "name": "FORMAT_CMD", "optional": false, "description": "Run formatter (or the linter's autofix if no dedicated formatter)", "example": "npm run format" },
    { "name": "LINT_CMD", "optional": false, "description": "Run linter", "example": "npm run lint" },
    { "name": "TYPECHECK_CMD", "optional": true, "description": "Type-check (drop if the language is untyped)", "example": "npx tsc --noEmit" },
    { "name": "UNIT_TEST_CMD", "optional": true, "description": "Run unit tests", "example": "npm run test:unit" },
    { "name": "E2E_TEST_CMD", "optional": true, "description": "Run e2e tests", "example": "npm run test:e2e" },

    { "name": "CODE_FILE_GLOB", "optional": false, "description": "Shell case-pattern of formatted extensions (format.sh). Contains shell metacharacters — use init.sh (literal substitution), not a sed sweep.", "example": "*.ts | *.tsx | *.css" },
    { "name": "CODE_FILE_REGEX", "optional": false, "description": "Extended-regex of source extensions (check.sh). Contains regex metacharacters — use init.sh (literal substitution), not a sed sweep.", "example": "\\.(ts|tsx|css)$" }
  ]
}
