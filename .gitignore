# Local agent harness state materialized by .claude/hooks/session-start.sh.
# These hold machine-local settings and secrets and MUST stay out of version
# control. The application-security skill assumes these entries exist; keep them
# (and extend this file with the project's own ignores) during INIT.
/.claude/settings.local.json
.env.local
.env.*.local

# Node / Next.js
node_modules
.next
*.tsbuildinfo

# OpenNext / Wrangler build output and local state
.open-next
.wrangler

# Per-pull-request Wrangler config the preview-deployment workflow templates
# at deploy time; never a committed file.
wrangler.preview.json

# Test artifacts
coverage
/test-results
/playwright-report
/blob-report
/playwright/.cache
